import styled from 'styled-components';

const CartIcon = styled.button.attrs(() => ({
	type: 'button',
}))`
	background: none;
	border: none;
	color: #fff;
	cursor: pointer;
	margin: 0;
	padding: 0;
	opacity: 0.79;
`;

export default CartIcon;
